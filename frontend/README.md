# Frontend — AI Medical Chatbot

React + TypeScript + Vite + Tailwind CSS v4 client for the AI Medical Chatbot.

See the [project root README](../README.md) for the full architecture, setup, and
deployment guide. Quick start:

```bash
npm install
cp .env.example .env   # point VITE_API_URL at your backend
npm run dev
```
